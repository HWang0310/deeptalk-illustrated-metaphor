# V0.1 Original Visual Language Implementation Plan

> **For agentic workers:** Execute task-by-task with tests before implementation.

**Goal:** Add two original Track B visual systems, refined deterministic motion evidence, and a bounded model consistency study to the established benchmark renderer.

**Architecture:** Extend the SVG renderer with `b_paper_relay` and `b_object_theatre` without altering Track A semantics. A V0.1 runner selects comparable tracks/routes, adds timed motion evidence, and emits a focused comparison report.

**Tech Stack:** Python standard library, SVG, macOS `sips`, FFmpeg, unittest, built-in image generation.

**Spec:** `docs/plans/2026-08-27-v0.1-original-visual-language-design.md`

## Global Constraints

- Track A is frozen reference-only and remains marked `upstream-reference-only`.
- Track B candidates are original visual-language hypotheses, not a permanent brand or mascot.
- Chinese stays in deterministic SVG typography.
- Approved-still deterministic motion remains the delivery baseline.
- No DeepTalk Core changes and no final integration contract.

### Task 1: Candidate scene grammars

**Files:** `src/illustrated_metaphor/art.py`, `tests/test_art.py`

- [ ] Add a failing test that B1 contains a generic paper actor marker and B2 contains no character marker but has an object-theatre marker.
- [ ] Run the focused test and confirm import/assertion failure.
- [ ] Implement both coherent SVG systems with distinct palette, shape language, and state scaling.
- [ ] Re-run focused test green.

### Task 2: Refined motion plan

**Files:** `src/illustrated_metaphor/motion.py`, `tests/test_motion.py`

- [ ] Add a failing test for `reveal`, `focal_pan`, `object_emphasis`, and `hold` in the approved-still plan.
- [ ] Run focused test and confirm failure.
- [ ] Implement ordered timing metadata while preserving structured-hybrid and independent-keyframe routes.
- [ ] Re-run focused test green.

### Task 3: V0.1 comparable runner and QA

**Files:** `src/illustrated_metaphor/cli.py`, `src/illustrated_metaphor/qa.py`, `tests/test_cli.py`, `tests/test_qa.py`

- [ ] Add failing tests for 35 planned comparable assets and candidate identity QA.
- [ ] Implement `render_v01_comparison`; produce Track A baseline, V0 neutral control, and both Track B candidates.
- [ ] Render the full seven-case corpus and validate manifests/contact sheets/durations.

### Task 4: Model consistency probe and research record

**Files:** `docs/RESEARCH.md`, `docs/QUALITY.md`, `docs/BENCHMARKS.md`, `PROJECT_STATE.md`, `HANDOFF.md`

- [ ] Generate only two B1 scene states with a locked prompt and no image text.
- [ ] Visually inspect and record character/object/layout/style continuity separately from deterministic evidence.
- [ ] Update recommendation and remaining risk statements.

### Task 5: Verification and publication

- [ ] Run full tests, plugin validation, V0.1 render, duration/QA/repeatability checks, and clean-tree check.
- [ ] Commit and push `main`.
